import emailjs from "@emailjs/browser";

// integration of email.js

  const serviceId = "service_ukxbz2f";
  const templateId = "template_r7t30kf";
  const publicId = "";

  // create eventhandler function
  const addContactFormData = (e) => {
    e.preventDefault();
    // send al data in admin
    emailjs.sendFrom();
  };
